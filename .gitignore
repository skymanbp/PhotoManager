# Haskell / Stack
.stack-work/
dist-newstyle/
*.hi
*.o
# hpack 从 package.yaml 生成，单一源
photo-manager.cabal

# cc-memory plugin data (session-local, not project source)
memory/
.ccm/

# Local scratch
*.local.*

.ce/

# Rust / Tauri GUI（P4-2）：构建产物与 tauri 生成的 schema
gui/src-tauri/target/
gui/src-tauri/gen/
gui/src-tauri/binaries/


# 仓根的操作手册 PDF 等本机产物（2026-09-02 用户指令：手册放仓根、不入库）
/*.pdf
