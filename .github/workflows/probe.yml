name: probe
on: [workflow_dispatch, push]
jobs:
  who:
    runs-on: windows-latest
    steps:
      - run: whoami & whoami /priv & whoami /groups | findstr /i "admin S-1-16" & exit /b 0
        shell: cmd
